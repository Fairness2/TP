<?
	if (isset($galaxy))
	{
		$id = $galaxy->_id;
		$name = $galaxy->name;
		$size = $galaxy->size;
		$type = TypeOfGalaxy::findById($galaxy->type_id)->name;
		$cluster = Cluster::findById($galaxy->cluster_id)->name;
		include "upd_article.phtml";
	}
	else
		echo '<article class="midl"><h2>Что-то пошло не так</h2></article>';
?>


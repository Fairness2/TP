<?
	if (isset($black_hole))
	{
		$id = $black_hole->_id;
		$name = $black_hole->name;
		$weight = $black_hole->weight;
		$age = $black_hole->age;
		$galaxy = Galaxy::findById($black_hole->galaxy_id)->name;
		$type = TypeOfBlackHole::findById($black_hole->type_id)->name;
		include "upd_article.phtml";
	}
	else
		echo '<article class="midl"><h2>Что-то пошло не так</h2></article>';
?>


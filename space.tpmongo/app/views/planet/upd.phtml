<?
	if (isset($planet))
	{
		$id = $planet->_id;
		$name = $planet->name;
		$weight = $planet->weight;
		$solar_system = SolarSystem::findById($planet->solar_system_id)->name;
		$type = TypeOfPlanet::findById($planet->type_id)->name;
		include "upd_article.phtml";
	}
	else
		echo '<article class="midl"><h2>Что-то пошло не так</h2></article>';
?>

